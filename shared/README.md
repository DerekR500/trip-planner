# shared

Placeholder. This package will hold TypeScript types shared between `frontend/` and
`backend/` (e.g. trip and stop shapes, API request/response types) in a later milestone.

Nothing here yet — Milestone 1 is a connectivity skeleton only.
